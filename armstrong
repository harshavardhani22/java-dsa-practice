class Solution {
    public boolean isArmstrong(int n) {
        int original = n;
        int sum = 0;
        
        while (n > 0) {
            int dig = n % 10;
            sum = sum + (dig * dig * dig);  
            n = n / 10;
        }
        
        return original == sum;
    }
}
